# CaveCode Academy — Official Logo Pass 3

This is a visual refinement pass. The approved SVG remains byte-for-byte unchanged.

## Main homepage

- Adds a large official logo above the hero title.
- Centers the hero copy, action buttons, and terminal section.
- Removes white backing surfaces from every logo.
- Uses the current theme accent as a CSS `drop-shadow` glow.

## Learning Paths

- Centers the existing Learning Paths logo and heading.
- Leaves every Learning Path card and action unchanged.

## Course pages

- Keeps the official Academy logo and language identity.
- Removes the extra one-sentence course description from the sidebar brand block.
- Applies the same background-free theme glow.

## Shared pages

The existing logo is centered above the title on:

- Appearance / Settings
- Achievements
- Leaderboard
- Minigames

## Protected behavior

- The approved SVG is not edited.
- Learning Path cards remain byte-for-byte unchanged.
- Razor `@code` blocks remain byte-for-byte unchanged.
- No lessons, progress, XP, crystals, achievements, leaderboard logic, minigame logic, authentication, or Supabase behavior changes.
